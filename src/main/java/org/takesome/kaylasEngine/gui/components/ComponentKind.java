package org.takesome.kaylasEngine.gui.components;

/** Logical kind of an engine component definition. */
public enum ComponentKind {
    /** A single reusable component implementation. */
    BASIC,

    /** A reusable graph assembled from other catalog components. */
    COMPOSITE
}

# Local developer settings. Copy this file to local.properties and fill only locally.
# local.properties is ignored by git and must never be committed.

project.version=2.2.0-AURELIA
project.group=org.takesome
project.archivesBaseName=kaylas-ui-engine

# Optional publishing credentials are read from local.properties or CI environment.
# Keep real values only on your machine or in GitHub Actions secrets.
